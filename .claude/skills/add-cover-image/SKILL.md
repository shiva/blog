---
name: add-cover-image
description: Add an Unsplash cover image to a blog post — downloaded into static/images, checked into the repo, with attribution. Use when the user asks to add a cover image or cover photo to a post.
---

Read `.github/prompts/add-cover-image.prompt.md` at the repo root and follow the workflow defined there (ignore its frontmatter). Where it says "web search / fetching", use WebSearch or WebFetch; run the `curl` and `git add` steps via Bash from the repo root. "Currently open in the editor" means the file noted in the latest ide_opened_file reminder, if any.
